{
	"name": "docs-jiosaavn-api",
	"version": "0.1.0",
	"description": "Docs for JioSaavn API in TypeScript and Rust programming language 🦀",
	"scripts": {
		"dev": "retype start",
		"build": "retype build",
		"serve": "retype serve",
		"fmt": "prettier --write \"**/*.{md,yml}\" --cache",
		"prepare": "husky install"
	},
	"dependencies": {
		"retypeapp": "^4.5.3"
	},
	"devDependencies": {
		"@commitlint/cli": "^20.5.0",
		"@commitlint/config-conventional": "^20.5.0",
		"husky": "^9.1.7",
		"lint-staged": "^16.4.0",
		"prettier": "^3.8.2"
	}
}
